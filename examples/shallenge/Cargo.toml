[package]
name = "shallenge"
version = "0.1.0"
edition = "2024"

[dependencies]
llvm-metal-compiler = { path = "../../compiler" }
llvm-metal-kernel = { path = "../../kernel" }
llvm-metal-runtime = { path = "../../runtime" }
shallenge-kernel = { path = "kernel" }
