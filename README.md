# llvm-metal (v2)

Compile Rust GPU kernels to Metal libraries for Apple GPUs. This branch is a
rewrite from an empty tree; nothing here works yet. The design, its questions
and its scope are in issue #30. The previous compiler is on `master`.

Read `AGENTS.md` before changing anything.
