[package]
name = "llvm-metal-abi"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
llvm-metal-kernel = { path = "../kernel" }
serde = { version = "1", features = ["derive"] }
